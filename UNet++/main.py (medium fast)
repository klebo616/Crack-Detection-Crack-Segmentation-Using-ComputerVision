# main_compact.py
import os, glob, csv
import numpy as np
import torch
from torch import nn, optim
from torch.utils.data import Dataset, DataLoader
from PIL import Image
from tqdm import tqdm
import matplotlib.pyplot as plt

import albumentations as A
import torchvision.transforms as T

from unet_model import UNetPP  # keep your model import

# -------------------- CONFIG --------------------
CFG = dict(
    lr=3e-4, wd=1e-4, batch_size=12, epochs=15, img_size=224,
    use_amp=True, channels_last=True, num_workers=4,
    model_path=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\models\unet_model_bruno1.pth",
    curve_png=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\models\loss_curves_bruno1.png",
    csv_path=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\models\training_log_bruno1.csv",
    train_pos=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\patches\train\Positive",
    train_neg=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\patches\train\Negative",
    train_gt=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\bw\train",
    val_pos=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\patches\val\images\Positive",
    val_neg=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\patches\val\images\Negative",
    val_gt=r"C:\Users\bob1k\Desktop\MASTER\Thesis\UNetPP_Real\dataset_Crack_Extended\bw\val",
    exts=(".jpg", ".JPG", ".png", ".PNG"),
    early_patience=1000,
    scheduler_patience=1, scheduler_factor=0.5,
    check_aug_diff=True,  # enable to print mean aug diff + stats
    use_pos_weight=True, pos_weight_value=1.0,
)

# -------------------- UTILS --------------------
def ensure_dir(path): os.makedirs(os.path.dirname(path), exist_ok=True)

def csv_append_row(path, row_dict):
    ensure_dir(path)
    write_header = not os.path.exists(path)
    with open(path, "a", newline="") as f:
        w = csv.DictWriter(f, fieldnames=list(row_dict.keys()))
        if write_header: w.writeheader()
        w.writerow(row_dict)

def glob_all(folder, exts):
    out = []
    for e in exts: out += glob.glob(os.path.join(folder, f"*{e}"))
    return out

def find_mask_for(stem, gt_dir, exts):
    cands = [os.path.join(gt_dir, f"bw_{stem}{e}") for e in exts] + \
            [os.path.join(gt_dir, f"{stem}{e}") for e in exts]
    for p in cands:
        if os.path.exists(p): return p
    return None

# -------------------- DATA --------------------
class PatchDataset(Dataset):
    def __init__(self, pairs, img_size=224):
        self.pairs = pairs; self.img_size = img_size
    def __len__(self): return len(self.pairs)
    def __getitem__(self, i):
        ip, mp = self.pairs[i]
        img = Image.open(ip).convert("RGB")
        if self.img_size: img = img.resize((self.img_size, self.img_size))
        x = np.asarray(img, dtype=np.float32)/255.0
        x = torch.from_numpy(x.transpose(2,0,1))
        if mp is None:
            _, H, W = x.shape
            y = torch.zeros((1,H,W), dtype=torch.float32)
        else:
            m = Image.open(mp).convert("L")
            if self.img_size: m = m.resize((self.img_size, self.img_size))
            m = (np.asarray(m, dtype=np.uint8) > 0).astype(np.float32)[None,...]
            y = torch.from_numpy(m)
        return x, y

class AlbumentationsWrapper(Dataset):
    def __init__(self, base_ds, geo_tfm):
        self.base=base_ds; self.geo_tfm=geo_tfm
    def __len__(self): return len(self.base)
    def __getitem__(self,i):
        x,y = self.base[i]
        img = x.numpy().transpose(1,2,0)
        msk = y.numpy().transpose(1,2,0)
        out = self.geo_tfm(image=img, mask=msk)
        xi = torch.from_numpy(out["image"].transpose(2,0,1)).float()
        yi = torch.from_numpy(out["mask"].transpose(2,0,1)).float().clamp(0,1)
        return xi, yi

class AddGaussianNoise(torch.nn.Module):
    def __init__(self, std=0.04): super().__init__(); self.std=std
    def forward(self, x): return (x + torch.randn_like(x)*self.std).clamp(0,1)

class AugmentWrapper(Dataset):
    def __init__(self, base_ds, tfm): self.base=base_ds; self.tfm=tfm
    def __len__(self): return len(self.base)
    def __getitem__(self,i):
        x,y = self.base[i]; return self.tfm(x), y

def build_pairs(pos_dir, neg_dir, gt_dir, exts):
    pairs=[]
    for ip in glob_all(pos_dir, exts):
        stem=os.path.splitext(os.path.basename(ip))[0]
        mp=find_mask_for(stem, gt_dir, exts)
        if mp: pairs.append((ip, mp))
        else: print(f"[WARN] Missing mask for: {ip}")
    if os.path.isdir(neg_dir):
        for ip in glob_all(neg_dir, exts): pairs.append((ip, None))
    return pairs

# -------------------- AUG BUILDER --------------------
def build_geo_tfm():
    return A.Compose([
        A.HorizontalFlip(p=0.4),
        A.ShiftScaleRotate(shift_limit=0.02, scale_limit=0.10, rotate_limit=10, border_mode=0, p=0.6),
    ], additional_targets={'mask':'mask'})

def build_img_tfm():
    return T.Compose([
        T.RandomApply([T.ColorJitter(brightness=0.43, contrast=0.4)], p=0.5),
        T.RandomApply([T.GaussianBlur(3, (0.6, 1.0))], p=0.2),
        T.RandomApply([AddGaussianNoise(std=0.05)], p=0.3),
    ])


# -------------------- METRICS --------------------
@torch.no_grad()
def dice_iou_from_logits(logits, target, eps=1e-7):
    probs = torch.sigmoid(logits)
    preds = (probs > 0.5).float()
    inter = (preds * target).sum(dim=(1,2,3))
    pred_sum = preds.sum(dim=(1,2,3))
    targ_sum = target.sum(dim=(1,2,3))
    dice = (2*inter + eps) / (pred_sum + targ_sum + eps)
    iou  = (inter + eps) / (pred_sum + targ_sum - inter + eps)
    return dice.mean().item(), iou.mean().item()

# -------------------- TRAIN/VAL --------------------
def train_one_epoch(model, loader, device, scaler, criterion, optimizer, channels_last):
    model.train(); total=0.0; n=0
    for img,mask in tqdm(loader, desc="Train", leave=False):
        if channels_last: img = img.to(memory_format=torch.channels_last)
        img,mask = img.to(device, non_blocking=True), mask.to(device, non_blocking=True)
        optimizer.zero_grad(set_to_none=True)
        with torch.amp.autocast(device_type="cuda", enabled=(device=="cuda")):
            pred = model(img); loss = criterion(pred, mask)
        scaler.scale(loss).backward()
        scaler.step(optimizer); scaler.update()
        total += loss.item(); n += 1
    return total/max(n,1)

@torch.no_grad()
def evaluate(model, loader, device, scaler, criterion, channels_last):
    model.eval(); total=0.0; n=0; dice_list=[]; iou_list=[]
    for img,mask in tqdm(loader, desc="Val", leave=False):
        if channels_last: img = img.to(memory_format=torch.channels_last)
        img,mask = img.to(device, non_blocking=True), mask.to(device, non_blocking=True)
        with torch.amp.autocast(device_type="cuda", enabled=(device=="cuda")):
            pred = model(img); loss = criterion(pred, mask)
        total += loss.item(); n += 1
        d,i = dice_iou_from_logits(pred, mask)
        dice_list.append(d); iou_list.append(i)
    return total/max(n,1), float(np.mean(dice_list)), float(np.mean(iou_list))

def fit():
    torch.backends.cudnn.benchmark = True
    torch.set_float32_matmul_precision("high")

    device = "cuda" if torch.cuda.is_available() else "cpu"
    print(f"[INFO] cuda={torch.cuda.is_available()} device={torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'cpu'}")

    train_pairs = build_pairs(CFG["train_pos"], CFG["train_neg"], CFG["train_gt"], CFG["exts"])
    val_pairs   = build_pairs(CFG["val_pos"], CFG["val_neg"], CFG["val_gt"], CFG["exts"])
    print(f"[INFO] Train: {len(train_pairs)} | Val: {len(val_pairs)}")

    base_train = PatchDataset(train_pairs, img_size=CFG["img_size"])
    base_val   = PatchDataset(val_pairs,   img_size=CFG["img_size"])

    train_ds = AlbumentationsWrapper(base_train, build_geo_tfm())
    train_ds = AugmentWrapper(train_ds, build_img_tfm())
    val_ds   = base_val

    # ---- Aug diff diagnostic ----
    if CFG["check_aug_diff"]:
        raw_x,_ = base_train[0]
        tfm = build_img_tfm()
        diffs = [(tfm(raw_x.clone())-raw_x).abs().mean().item() for _ in range(100)]
        thr = 0.05
        affected = sum(d>thr for d in diffs)/len(diffs)
        coef = np.mean(diffs) / (np.std(diffs) + 1e-8)
        print(f"[AUG CHECK] avg diff={np.mean(diffs):.3f} | std={np.std(diffs):.3f} | coef={coef:.3f} | affected(>{thr})={affected*100:.1f}%")

    kw = dict(num_workers=CFG["num_workers"], pin_memory=True, persistent_workers=True)
    train_loader = DataLoader(train_ds, batch_size=CFG["batch_size"], shuffle=True, **kw)
    val_loader   = DataLoader(val_ds,   batch_size=CFG["batch_size"], shuffle=False, **kw)

    model = UNetPP(in_channels=3, num_classes=1)
    if CFG["channels_last"]: model = model.to(memory_format=torch.channels_last)
    model = model.to(device)

    if CFG["use_pos_weight"]:
        pos_w = torch.tensor([CFG["pos_weight_value"]], device=device)
        criterion = nn.BCEWithLogitsLoss(pos_weight=pos_w)
    else:
        criterion = nn.BCEWithLogitsLoss()

    optimizer = optim.AdamW(model.parameters(), lr=CFG["lr"], weight_decay=CFG["wd"])
    scheduler = optim.lr_scheduler.ReduceLROnPlateau(optimizer, factor=CFG["scheduler_factor"], patience=CFG["scheduler_patience"])
    scaler = torch.amp.GradScaler("cuda", enabled=(device=="cuda" and CFG["use_amp"]))

    hist = {"epoch":[], "train_loss":[], "val_loss":[], "dice":[], "iou":[]}
    best_val = float("inf"); bad=0
    best_path = CFG["model_path"].replace(".pth","_best.pth")

    for ep in tqdm(range(1, CFG["epochs"]+1), desc="Epochs"):
        tr = train_one_epoch(model, train_loader, device, scaler, criterion, optimizer, CFG["channels_last"])
        vl, dsc, iu = evaluate(model, val_loader, device, scaler, criterion, CFG["channels_last"])
        scheduler.step(vl)

        hist["epoch"].append(ep); hist["train_loss"].append(tr); hist["val_loss"].append(vl)
        hist["dice"].append(dsc); hist["iou"].append(iu)
        csv_append_row(CFG["csv_path"], {"epoch":ep, "train_loss":float(tr), "val_loss":float(vl), "dice":float(dsc), "iou":float(iu)})

        print(f"Epoch {ep:03d} | train={tr:.4f} | val={vl:.4f} | dice={dsc:.3f} | IoU={iu:.3f}")

        if vl < best_val - 1e-4:
            best_val = vl; bad=0
            ensure_dir(best_path); torch.save(model.state_dict(), best_path)
            print(f"[INFO] saved best -> {best_path}")
        else:
            bad += 1
            if bad >= CFG["early_patience"]:
                print(f"[INFO] early stop at epoch {ep}")
                break

    ensure_dir(CFG["model_path"])
    torch.save(model.state_dict(), CFG["model_path"])
    print(f"[INFO] saved last -> {CFG['model_path']}")

    # ---- Unified Plot (Loss + Dice + IoU) ----
    try:
        ensure_dir(CFG["curve_png"])
        plt.figure(figsize=(8,5))
        plt.plot(hist["epoch"], hist["train_loss"], label="Train Loss", linewidth=2)
        plt.plot(hist["epoch"], hist["val_loss"], label="Val Loss", linewidth=2)
        plt.plot(hist["epoch"], hist["dice"], '--', label="Dice", alpha=0.7)
        plt.plot(hist["epoch"], hist["iou"], '--', label="IoU", alpha=0.7)
        plt.xlabel("Epoch"); plt.ylabel("Metric Value")
        plt.title("Training Progress (Loss, Dice, IoU)")
        plt.legend(); plt.grid(True, linestyle=":")
        plt.tight_layout(); plt.savefig(CFG["curve_png"], dpi=150); plt.close()
        print(f"[INFO] curves -> {CFG['curve_png']}")
    except Exception as e:
        print(f"[WARN] plot failed: {e}")

if __name__ == "__main__":
    fit()
